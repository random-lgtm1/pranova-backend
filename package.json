{
  "name": "pranova-backend",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "pip install -r requirements.txt || python -m pip install -r requirements.txt || echo 'Python dependencies installation skipped'",
    "build": "pip install -r requirements.txt || python -m pip install -r requirements.txt || echo 'Python dependencies installation skipped'",
    "start": "node server.js"
  },
  "dependencies": {
    "@google/genai": "^2.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "googlethis": "^1.8.0",
    "openai": "^6.42.0"
  }
}
